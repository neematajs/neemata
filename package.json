{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "build:watch": "pnpm -r run build:watch",
    "link": "pnpm -r run link",
    "check:lint": "biome lint .",
    "check:fmt": "biome format .",
    "fmt": "biome check --apply ."
  },
  "devDependencies": {
    "@biomejs/biome": "1.7.1",
    "@types/bun": "^1.1.0"
  },
  "trustedDependencies": [
    "@biomejs/biome"
  ],
  "peerDependencies": {
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "typescript": "^5.4.5"
  },
  "workspaces": [
    "./packages/*"
  ],
  "packageManager": "pnpm@9.0.6"
}
