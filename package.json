{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun --filter '*' build",
    "build:watch": "bun --filter '*' build:watch",
    "link": "bun --filter '*' link",
    "check:lint": "biome lint .",
    "check:fmt": "biome format .",
    "fmt": "biome check --apply ."
  },
  "devDependencies": {
    "@biomejs/biome": "1.7.1",
    "@types/bun": "^1.1.0"
  },
  "trustedDependencies": [
    "@biomejs/biome"
  ],
  "peerDependencies": {
    "typescript": "^5.4.5"
  },
  "workspaces": ["packages/*"],
  "dependencies": {
    "typescript": "^5.4.5"
  }
}