{
  "name": "@neematajs/common",
  "version": "1.0.0",
  "type": "module",
  "module": "index.ts",
  "publishConfig": {
    "module": "./dist/index.js",
    "types": "./index.ts"
  },
  "scripts": {
    "build": "bun build index.ts $(pnpm --silent -w run entrypoints $(pwd) lib) --outdir=dist --splitting --external=\\* --sourcemap=external --target=browser",
    "type-check": "tsc --noEmit"
  }
}
