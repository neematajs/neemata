export const kApplicationConfig: unique symbol = Symbol.for(
  'neemata:ApplicationConfig',
)
export type kApplicationConfig = typeof kApplicationConfig

export const kApplicationHostDefinition: unique symbol = Symbol.for(
  'neemata:ApplicationHostDefinition',
)
export type kApplicationHostDefinition = typeof kApplicationHostDefinition
