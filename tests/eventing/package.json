{
  "name": "@nmtjs/tests-eventing",
  "private": true,
  "type": "module",
  "scripts": {
    "build:deps": "tsgo -b ../../packages/eventing/tsconfig.build.json",
    "test": "pnpm run build:deps && vitest run --config vitest.config.ts",
    "check:type": "tsgo -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@nmtjs/core": "workspace:*",
    "@nmtjs/eventing": "workspace:*",
    "@nmtjs/type": "workspace:*",
    "vitest": "4.1.7"
  },
  "devDependencies": {
    "@types/node": "^24"
  }
}
