{
  "name": "@nmtjs/tests-neem",
  "private": true,
  "type": "module",
  "scripts": {
    "build:deps": "tsgo -b ../../packages/neem/tsconfig.build.json ../../packages/application/tsconfig.build.json",
    "test": "pnpm run build:deps && vitest run --config vitest.config.ts",
    "check:type": "tsgo -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@nmtjs/application": "workspace:*",
    "@nmtjs/core": "workspace:*",
    "@nmtjs/neem": "workspace:*"
  }
}
